name: 'Main Release'

on:
  push:
    branches:
      - main

# Releases a canary tagged version of the packages
jobs:
  main-release:
    uses: ./.github/shared/release-job.yml
    with:
      changeset-commit-msg: 'Main release'
      changeset-pr-title: 'Update Versions'
    secrets: inherit
