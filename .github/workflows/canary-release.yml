name: 'Canary Release'

on:
  push:
    branches:
      - canary

# Releases a canary tagged version of the packages
jobs:
  canary-release:
    uses: ./.github/shared/release-job.yml
    with:
      commit-msg: 'Canary release'
      pr-title: 'Canary Release'
    secrets: inherit
