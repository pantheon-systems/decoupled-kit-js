.header {
	{{#if tailwindcss}}
	@apply prose;
	{{/if}}
	font-size: var(--6);
	margin-right: auto;
	margin-left: auto;
	margin-top: var(--20);
}

.headerTitle {
	text-align: center;
	margin-right: auto;
	margin-left: auto;
}
