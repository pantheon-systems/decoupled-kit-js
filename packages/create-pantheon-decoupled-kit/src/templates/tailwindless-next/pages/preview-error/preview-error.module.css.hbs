{{#if drupal}}
	.container {
		display: flex;
		flex-direction: column;
		margin: var(--20) auto 0;
		width: 50vw;
	}
	
	.container > h2 {
		text-align: center;
		font-size: var(--8);
		margin-bottom: var(--8);
	}

	.container > a {
		text-decoration: underline;
		font-size: var(--5);
	}

{{/if}}