# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
# Run Rennovate in CircleCI
# see https://circleci.com/developer/orbs/orb/daniel-shuy/renovate
orbs:
  renovate: daniel-shuy/renovate@3.1.0
# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  test:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: cimg/node:16.4
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - restore_cache:
          # See the configuration reference documentation for more details on using restore_cache and save_cache steps
          # https://circleci.com/docs/2.0/configuration-reference/?section=reference#save_cache
          keys:
            - node-deps-v1-{{ .Branch }}-{{checksum "pnpm-lock.yaml"}}
      - run:
          name: install pnpm
          command: |
            curl -f https://get.pnpm.io/v6.16.js | sudo node - add --global pnpm@6
            pnpm config set store-dir ~/.pnpm-store
      - run:
          name: install packages
          command: pnpm install --ignore-scripts
      - save_cache:
          key: node-deps-v1-{{ .Branch }}-{{checksum "pnpm-lock.yaml"}}
          paths:
            - ~/.pnpm-store
      - run:
          name: Run Tests
          command: pnpm test
      - run:
          name: Build Packages
          command: pnpm build:pkgs

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  orb-free-workflow:
    jobs:
      - test
  renovate:
    jobs:
      - renovate/self-hosted:
          config_file_path: renovate.json
    nightly:
      triggers:
        - schedule:
            # set to hourly for testing
            cron: 0 * * * *
            filters:
              branches:
              # filter to only this branch for testing
                only:
                  - DB-1787-renovatebot

